# Velocity (experimental, Docker)

The modern Java proxy: many backend servers behind one address. Declared and
version-aware, Docker-only for now like Fabric and Forge, for the same
reason: no Docker engine in the panel yet.

When it boots, the Minekube proxy connector applies, and one public address
fronts your whole network. Until then, run single servers directly.
