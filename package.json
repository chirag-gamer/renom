{
  "name": "renom",
  "private": true,
  "version": "0.1.0-alpha.1",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/contracts && npm run build -w apps/panel",
    "lint": "eslint . && prettier --check .",
    "typecheck": "npm run build -w packages/contracts && npm run typecheck --workspaces --if-present",
    "test": "npm run build -w packages/contracts && npm run test --workspace apps/panel",
    "dev": "npm run dev --workspace apps/panel",
    "sbom": "cyclonedx-npm --omit=dev --output-file sbom.json"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-npm": "^2.0.0",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^9.1.2",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.68.0"
  }
}
